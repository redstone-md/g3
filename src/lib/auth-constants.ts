/** Shared auth constants safe to import from the proxy (no server-only deps). */
export const SESSION_COOKIE = "ribbon_session";

/** Fast SSR hint for the active theme; DB is the source of truth. */
export const THEME_COOKIE = "ribbon_theme";
